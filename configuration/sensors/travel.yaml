# here travel time sensor
  - platform: here_travel_time
    api_key: !secret HereAPI
    name: Reistijd naar huis
    origin_entity_id: !secret devicetracker_auto
    destination_entity_id: zone.home
    scan_interval: 600
    traffic_mode: true
    
#  - platform: template
#    sensors:
#      dest_address:
#         value_template: >-
#            {%- if is_state("device_tracker.jl976r.state", "Home")  -%}
#              zone.work
#            {%- else -%}
#              zone.home
#            {%- endif %}